# === Big data / output dirs ===
__trash/

# === Compiled / checkpoint artifacts ===
__pycache__/
*.pyc
.ipynb_checkpoints/
*xrd/_trash/

# === Database files (AGOX results) — large binary, regenerated ===
*.db

# === Log files (run output, not source) ===
*.log

# === Ray session dirs (large, ephemeral) ===
ray/

# === Big notebooks (embedded outputs, large) ===
_analysist/main_analyst.ipynb
_analysist/main_test.ipynb

# === _run: track code + docs, skip large artifacts ===
_run/**/*.png
_run/*/backups/

# === Generated AGOX simulation artifacts (regenerable; code/docs kept, data excluded) ===
# Raw results, logs, and structure files
*.txt
*.out
*.csv
*.xsf

# Run-output directories (nested sampling / novelty filter / generated structures)
generated_structures/
ns_output_allseeds/
novel_output/
side_by_side/

# Ray/dask transient dotfiles
.d[0-9]*

# === Large results / output dirs (regenerated, not source) ===
_analysist/0_analy/
_analysist/1_result/
_analysist/_style_sanity.png
_archive/

# === Archived skill snapshots (old flat copies superseded by _skills/) ===
_skills_archive_flat_20260825/

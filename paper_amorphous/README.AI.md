# README.AI.md — paper_amorphous (agent spec)

## Purpose
Initialize the `scientific-paper-writing` workflow on the amorphous Pt(P) system. Amorphous generation (GOFEE/AGOX) is complete; the next goal is a spin Hall conductivity (SHC) calculation via FLAPW. Paper1 is scaffolded now (amorphous generation + methods), with the SHC section as a placeholder to fill once results land.

## File layout
```
paper_amorphous/
├── AGENTS.md            # governing rules (read first)
├── README.md            # human overview
├── README.AI.md         # this file
├── LOG.md               # append-only action log
├── TUTORIAL.md          # reproduction guide
├── VERSIONS.md          # per-file __version__ manifest
├── papers/
│   └── paper1/            # main manuscript (RevTeX 4.2)
│       ├── main.tex
│       ├── sections/      # 01_abstract … 06_ack_dataavail (.tex per section)
│       ├── figures/
│       ├── references.bib
│       ├── CLAIMS.md
│       ├── paper_status.md
│       └── supplementary/ # SI: article.tex (SPIE), report.bib, spieman.cls, spiejour.bst
├── codes/               # figure/analysis code (to be written)
├── analysis/            # regenerated figures + result JSONs (to be produced)
├── data/                # raw results (read-only reference)
│   ├── 17_PPt/          # Pt–P: 0_plus5cell(+5%), 1_plus0cell(+0%), 2_plus3cell(+3%); 3_plus10cell(+10%) EXCLUDED
│   ├── 11_bTa/          # B in Ta (SI discussion)
│   └── 16_bW/           # B in W (SI discussion)
└── tmp/                 # FLAPW 4.58 + ELPA install; SHC example (read-only reference)
```

## Data model (Pt–P, `data/17_PPt/`)
- **Host:** fcc Pt, a = 3.975534 Å, 3×3×3 supercell (Pt₁₀₈). P interstitials added via `generate_interstitial_alloy.py`; count `B = (C·A)/(1−C)`.
- **Cell families** (from `alloy_structure.xsf` PRIMVEC): `0_plus5cell` 12.523 Å (+5%), `1_plus0cell` 11.927 Å (+0%), `2_plus3cell` 12.284 Å (+3%), `3_plus10cell` 13.119 Å (+10%, **excluded**). Note: `main.py` `SCALE_CELL=1.05` is stale for the +0/+3 families — trust the xsf cells.
- **Compositions:** 3×3×3 leaves = Pt₁₀₈ + P interstitials (20P = Pt₁₀₈P₂₇, 30P = Pt₁₀₈P₄₆); `0_PPt_4x4_20P` = Pt₂₅₆P₆₄ (n=320).
- **AGOX pipeline:** `RattleGenerator` + `AmorphStructRandomize` + `AmorphPermutationGenerator`; GPR (RBF kernel, Repulsive prior) + LowerConfidenceBoundAcquisitor; GPAW LCAO/dzp/PBE, kpts (1,1,1); `LocalOptimizationEvaluator`; per-seed `1_db/db_*.db`.
- **Analysis:** `run_analysis_indices.py` per leaf (from `_run/0_lcb/2_analysist`), `iteration >= 10` filter, `--e-max 0.5` for Pt–P. XRD crystallinity via `xrd_simulate_crystallinity.py` (see `data/17_PPt/DISCUSSION.md`).

## SHC workflow (in progress)
- **Example:** `tmp/SHC Calculation/HEA_SHC_Auto_Python_FLAPW/` — `main.py` (SCF → SOC → optics → xoptics via `flapw.py` ASE `FLAPW` calculator), `job_genkai_mpi.sh` (conda `flapw_2`, pjsub, 8 procs).
- **Input:** a `.traj` file of structures (`traj_0.traj` in the example). Which amorphous structures feed the SHC (best-so-far vs representative set vs crystalline reference) is **TBD — clarify later**.
- **Env:** `flapw_2` conda env; HPC via pjsub (`a-pj24001864`).

## Expected inputs/outputs
- **Input:** `data/17_PPt/<family>/<leaf>/seed_*/1_db/db_*.db`; SHC results (pending).
- **Output:** `analysis/figures/*.png` + result JSONs; `papers/paper1/main.pdf` + `supplementary/article.pdf`.

## Error handling & edge cases
- `3_plus10cell` (+10%) family is **excluded** by scope (per owner + `DISCUSSION.md`).
- `main.py` `SCALE_CELL` constants are stale for +0/+3 families — use the xsf cell dimensions.
- High-energy XRD windows have few structures (e.g. n=2–3) — least reliable CI values; `--ci-x-data-max 0.25` plots exclude them.
- SHC section must remain a placeholder until the calculation is done — no SHC numbers/claims before then.

## Provenance
Data generated by the GOFEE/AGOX GPR+LCB search (Pt–P focus system, `17_PPt`). Analysis scripts live in `data/17_PPt/scripts/` and `_run/0_lcb/2_analysist`. SHC example from `tmp/SHC Calculation/HEA_SHC_Auto_Python_FLAPW/`.

# PROMPTS.md — Future-work prompt log

Shared grammar notes + every future-work prompt the owner drops in, newest first.

## Grammar notes

Shared, concept-based fixes applied to prompt entries below. Fold in, don't
duplicate per prompt.

1. **Formatting that breaks machine-readable content** — paths/identifiers split
   mid-token by line wraps. Keep paths/filenames on one line, intact, in backticks.
2. **Incomplete clauses** — missing verb/object/noun.
3. **Non-idiomatic phrasing** — unnatural preposition/filler.
4. **Redundant / misplaced comma** — comma splitting a verb from its object.
5. **Misspelling / dangling punctuation**.
6. **Terminology / clarity** — a loosely-used term pinned to its precise definition.

<!-- New processed entries go ABOVE this line, newest first. -->

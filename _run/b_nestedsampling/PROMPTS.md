# PROMPTS — Project b_nestedsampling prompt log + grammar concept system

This file logs every prompt the owner adds for future work, each marked with a
**flag code** (`YYYYMMDD_HHMM`, local time) and cleaned into a consistent grammar.
Newer prompts sit **above** older ones. See `AGENTS.md` §3b for the full rules.

> Scaffold created on project setup (2026-08-26). No future-work prompts logged
> yet. When a prompt is added, give it a `# FLAG: <YYYYMMDD_HHMM>` heading with the
> two grammar blocks below it and fold any fixes into the shared `## Grammar notes`.

## Grammar notes

Generalized concepts for grammar fixes across prompts. (No instances yet.)
